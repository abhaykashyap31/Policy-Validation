# Booking Module

Reserved for booking workflows after a travel request is approved. No booking API is exposed yet; the request status currently reaches `PENDING_APPROVAL` or `FLAGGED` after policy validation.
