# Common Module

Contains shared enums, exception handling, and API error responses.

The global exception handler returns structured `400`, `404`, and `409` responses for invalid input, missing resources, and database uniqueness conflicts.
